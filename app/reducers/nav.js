import { NavigationActions } from 'react-navigation';
import {
    USERNAME_CHANGED,
    PASSWORD_CHANGED,
    SERVERIP_CHANGED,
    SERVERIP_FAILED,
    LOGIN_SUCCESS,
    LOGIN_INIT,
    SERVER_NOT_REACHABLE,
    LOGGED_IN
} from '../actions/types';     

export default (state = {} , action ) => {   
    return state;
}
